# 计划：工作区还原 MiniMapStatus.html 到 6f6d0a0 备份基线

## 目标
用户已明确指令：先把工作区还原，丢弃 PC 立绘布局优化引入的渲染失败修改，恢复到 `6f6d0a0 backup: backup before pc portrait optimization` 的可渲染版本。

## 现状
- HEAD = 6f6d0a0 即为目标基线，工作区 `MiniMapStatus.html` 含约 209 行未提交优化（头部 lightbox/buildPortraitHTML、CSS 双栏、静态 modal、createCharacterCard 双栏重构）
- `buildPortraitHTML` 作用域泄露导致 `ReferenceError` 是渲染失败主因
- `git status` 显示仅 `MiniMapStatus.html` 被修改

## 执行步骤（批准后立即执行）
1. 只读复核：`git status` / `git diff HEAD --stat` 确认待丢弃范围
2. 可选备份（建议）：`git diff HEAD > pc-optimization-failed.patch` 留档（如需）
3. 执行还原（二选一）：
   ```
   git -C "D:/Project/MiniMapStatus" restore MiniMapStatus.html
   ```
4. 验证：`git status` 应为 clean，`git diff HEAD --stat` 为 0；抽检 `buildPortraitHTML` 恢复为 `40vh/7000px` 带羽化版本，`character-portrait-col` / `pt-lightbox-modal` 等新增内容已移除
5. 报告结果

## 风险
- 未备份直接还原会丢失当前失败优化，可通过 patch 备份规避
- 仅影响工作区，不改动提交历史

批准后立即执行还原并验证。
